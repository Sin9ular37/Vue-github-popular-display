# GitHub API 配置
# 如果不设置，将直接使用 GitHub API
# 如果设置代理服务器，指向代理地址
VITE_API_BASE=http://localhost:5174/github

# 或者直接使用 GitHub API（可能遇到速率限制）
# VITE_API_BASE=https://api.github.com 

# 翻译服务代理（可选），指向本地或部署后的翻译端点
# VITE_TRANSLATE_ENDPOINT=http://localhost:5174/translate
